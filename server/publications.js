Meteor.publish("chatrooms",function(){    // TODO Limit chat room publications to those that the user may actually join    return ChatRooms.find({});});Meteor.publish("chatroomsinvites",function(){    // TODO Limit chat room invite publications to those addressed to the user    return ChatRoomsInvites.find({});});Meteor.publish("onlusers",function(){    return Meteor.users.find({"status.online":true},{username:1});})